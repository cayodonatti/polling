import React from "react";
import "./flex.css";

export const Center = ({ children }) => (
  <div className="center">{children}</div>
);
export const CenterHorizontal = ({ children }) => (
  <div className="centerHorizontal">{children}</div>
);
